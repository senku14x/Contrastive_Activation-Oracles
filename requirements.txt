# Inference-only stack for the feasibility study (Stage 0/1).
# Pin to the CoT Oracle's training stack where it matters (transformers/peft).
# torch is installed separately per your CUDA build; flash-attn is optional.
transformers>=4.55,<5
peft>=0.17,<0.19
torch>=2.6
numpy<2
scikit-learn>=1.3
# Optional (not needed for stage0_repro.py or the experiment runtime):
#   flash-attn         # per-CUDA, install with --no-build-isolation
